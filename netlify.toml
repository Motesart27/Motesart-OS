[build]
  command = "npm run build"
  publish = "dist"

# Netlify functions handle /api/* directly — no Railway proxy
# SOM Railway backend is called directly from the frontend via VITE_API_URL

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
